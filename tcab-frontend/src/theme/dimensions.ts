import { spacingScale } from './spacing'

const dimensions = {
  formItems: {
    borderRadius: `${spacingScale(0.25)}px`,
    height: `${spacingScale(6)}px`,
  },
  icon: {
    activeHeight: `${spacingScale(2)}px`,
    normalHeight: `${spacingScale(2)}px`,
  },
  borderRadius: `${spacingScale(1)}px`,
  lineHeight: `${spacingScale(2.5)}px`,
  drawerWidth: 250,
}

export default dimensions
